# Chrome Extension (Manifest V3) Starter Template

## Stack
- **Manifest:** Version 3 (`manifest.json`)
- **Build Tool:** Vite + CRXJS plugin
- **UI:** React + Tailwind CSS (Popup / SidePanel / Options)
- **Background:** Service Worker (`background.ts`)
- **Content Scripts:** Isolated DOM manipulation (`content.ts`)
